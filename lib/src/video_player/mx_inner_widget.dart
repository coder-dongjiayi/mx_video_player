import 'package:flutter/material.dart';

/// 缓冲指示器
class MXInnerBuffer extends StatelessWidget {
  const MXInnerBuffer({Key? key, required this.child}) : super(key: key);
  final Widget child;
  @override
  Widget build(BuildContext context) {
    return child;
  }
}

/// 视频播放成功
class MXInnerSuccess extends StatelessWidget {
  const MXInnerSuccess({Key? key, required this.child}) : super(key: key);
  final Widget child;
  @override
  Widget build(BuildContext context) {
    return child;
  }
}
